{
  "//": [
    "Development-only overrides. Still no secrets: the connection string and signing key",
    "come from .env via docker compose, or from dotnet user-secrets when running on metal."
  ],
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",
      "Unify": "Debug"
    }
  },
  "Database": {
    "RunMigrationsOnStartup": true
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:3000",
      "http://localhost:3001"
    ]
  }
}
