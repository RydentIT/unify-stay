export default function HomePage() {
  return (
    <section className="flex flex-col gap-2">
      <h1 className="text-xl font-semibold">UnifyStay user portal</h1>
      <p className="text-sm">
        Scaffold build. The pages and API wiring are real; the backend auth handlers still
        return 501 until the auth module lands.
      </p>
    </section>
  );
}
